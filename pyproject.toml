[project]
name = "bita"
version = "0.2.1"
description = "A simple Python library for creating IIIF Presentation API manifests"
readme = "README.md"
authors = [{ name = "ysdmsnr" }]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
